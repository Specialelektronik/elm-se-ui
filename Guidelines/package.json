{
  "name": "guidelines",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/elm-live src/Main.elm --open --dir=public --start-page=index.html -- --output=public/app.js --debug"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "elm-live": "^4.0.1"
  }
}
